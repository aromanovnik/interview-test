import { BestsellerDirective } from './bestseller.directive';

describe('BestsellerDirective', () => {
  it('should create an instance', () => {
    const directive = new BestsellerDirective();
    expect(directive).toBeTruthy();
  });
});
